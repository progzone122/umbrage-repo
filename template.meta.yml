vendor: <Enter vendor>
model: <Enter model>
codename: <Enter codename>
chipset: <mt****>

versions:
  - id: 0
    default: true
    description: <Enter a few words about this files. Are there any restrictions? Vulnerable to the Carbonara exploit? Could it not work because of ARB?>
    files:
      da: <Move your files to the "files" directory and enter ONLY the filename. For example "MT6768_USER.bin">
      auth: <Do the same thing you did with DA. Comment it if it's not being used>
      preloader: <Do the same thing you did with DA. Comment it if it's not being used>
    checksums:
      da: <Use the "sha256sum" command to calculate the hash of a file>
      auth: <Use the "sha256sum" command to calculate the hash of a file>
      preloader: <Use the "sha256sum" command to calculate the hash of a file>
